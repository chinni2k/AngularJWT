/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ErrorHandler-2.oService } from './error-handler-2.o.service';

describe('Service: ErrorHandler-2.o', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ErrorHandler-2.oService]
    });
  });

  it('should ...', inject([ErrorHandler-2.oService], (service: ErrorHandler-2.oService) => {
    expect(service).toBeTruthy();
  }));
});
